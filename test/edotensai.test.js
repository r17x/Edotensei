import Edotensei from '../src'

describe('check add & remove static method in edotensei', () => {
  test('add in edotensei', () => {
    expect(Edotensei.add).toBeTruthy()
  })

  test('remove in edotensei', () => {
    expect(Edotensei.add).toBeTruthy()
  })
})
